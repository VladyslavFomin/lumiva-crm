-- Позволяет отдельным шаблонам писем (Почта → Письма, используются в автоматизациях) оборачиваться
-- в единый дизайн компании (tenants.ai_wrapper_email_template_id), как это уже работает для писем
-- ИИ-ассистента. По умолчанию false — все уже существующие шаблоны являются самостоятельными
-- HTML-документами и не должны получить обёртку поверх собственной вёрстки задним числом; новые
-- шаблоны сервис (email.service.ts createTemplate) явно проставляет true, если не указано иное.
ALTER TABLE email_templates ADD COLUMN IF NOT EXISTS "useWrapper" BOOLEAN NOT NULL DEFAULT false;
